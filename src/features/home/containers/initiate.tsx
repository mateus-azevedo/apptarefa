import React from 'react';
import {HomePanel} from '../components';

interface IInitiateContainerProps {}

const InitiateContainer = (props: IInitiateContainerProps) => {
  return <HomePanel />;
};

export default InitiateContainer;
