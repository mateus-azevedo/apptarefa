import React from 'react';

import {CreateTaskContainer} from '../containers';

interface ICreateTaskScreenProps {}

const CreateTaskScreen = (props: ICreateTaskScreenProps) => {
  return <CreateTaskContainer />;
};

export default CreateTaskScreen;
