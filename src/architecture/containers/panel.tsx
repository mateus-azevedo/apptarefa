import React from 'react';

import {PanelGeneric} from '../panels';

interface IPanelContainerProps {}

const PanelContainer = (props: IPanelContainerProps) => {
  return <PanelGeneric />;
};

export default PanelContainer;
