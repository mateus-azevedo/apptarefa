import React from 'react';
import {InitiateContainer} from '../containers';

interface IInitiateScreenProps {}

const InitiateScreen = (props: IInitiateScreenProps) => {
  return <InitiateContainer />;
};

export default InitiateScreen;
